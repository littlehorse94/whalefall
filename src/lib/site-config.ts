// The birthday page's private subdomain — its root serves /birthday
// directly (see proxy.ts) instead of the guild homepage. Shared so the
// rewrite and the guild's global audio toggle agree on the same host.
export const BIRTHDAY_HOST = 'xiaoxingxing.whalefall.club';
